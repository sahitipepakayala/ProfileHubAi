// Re-exported for convenience so components can import from "../hooks/*"
// consistently. Currently unused — pages import useAuth directly from
// context/AuthContext — but kept available for future refactors.
export { useAuth } from "../context/AuthContext";